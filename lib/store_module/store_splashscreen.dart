import 'package:flutter/material.dart';

class StoreSplashscreen extends StatelessWidget {
  const StoreSplashscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Icon(Icons.store, size: 100),
      ),
    );
  }
}
